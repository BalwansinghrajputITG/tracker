# Copy this file to .env and fill in your values before running docker-compose

# Cloudflare Tunnel / ngrok / public URLs for the React build
REACT_APP_API_URL=https://your-tunnel-url.trycloudflare.com/api/v1
REACT_APP_WS_URL=wss://your-ws-tunnel-url.trycloudflare.com/ws
